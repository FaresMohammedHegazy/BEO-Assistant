# All dependencies for planning/ have been merged into the root
# requirements.txt so `pip install -r requirements.txt` from the repo
# root installs everything needed to run mcp_server/, agent/, and
# planning/ together. This file is kept (empty) so a stray
# `pip install -r planning/requirements.txt` doesn't error out.