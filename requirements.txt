# Runtime dependencies for the BEO Assistant codebase
mcp
requests
jsonschema
python-dotenv
groq
sentence-transformers
scikit-learn
numpy

# Planning agent (Decomposition & Planning Lab) dependencies
langchain-groq>=0.2,<1
networkx>=3.4,<3.5
pydantic>=2.7,<3
langgraph>=0.2,<0.3
langgraph-checkpoint-sqlite>=2.0,<3
aiosqlite==0.21.0

# Optional development/testing packages do not need to be declared here unless
# the environment is expected to run the full regression suite from a fresh
# install. The repository uses unittest from the standard library.
pytest>=8,<9